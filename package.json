{
  "name": "sumo-webhook-receiver",
  "version": "1.0.0",
  "description": "Webhook receiver for sumo-api.com — handles newBasho, newMatches, and matchResults.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "csv-stringify": "^6.5.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
